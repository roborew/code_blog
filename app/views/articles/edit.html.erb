<article class="bg-white p-4 shadow rounded-lg">
<%= render 'article_header_links', current_page: 'edit' %>
  <div class="w-full">
    <% content_for :title, "Editing: #{@article.title}" %>
    <h1 class="text-2xl font-bold mb-4">Editing: <%= @article.title %></h1>
    <%= render "form", article: @article %>
  </div>
</article>


