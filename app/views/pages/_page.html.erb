<article class="bg-white p-4 shadow rounded-lg flex max-h-75" id="<%= dom_id page %>">
   <div>
       <h2 class="font-bold text-lg"><%= link_to page.title, page %></h2>
       <p class="text-gray-700"><%= page.content %>
        <%= link_to "Show this page", page, class: "text-blue-500" %>
   </div>
</article> 